from graph import prepare_data

if __name__ == "__main__":
    print("main started")
    # prepare_data("reddit")
    # prepare_data("wikipedia")
    # prepare_data("mooc")
    # prepare_data("lastfm")
